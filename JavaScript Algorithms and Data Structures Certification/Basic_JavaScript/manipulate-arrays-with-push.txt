// Example
var ourArray = ["Stimpson", "J", "cat"];
ourArray.push(["happy", "joy"]);
// ourArray now equals ["Stimpson", "J", "cat", ["happy", "joy"]]

// Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line.

myArray.push(["dog", 3]);

/*An easy way to append data to the end of an array is via the push() function.
.push() takes one or more parameters and "pushes" them onto the end of the array.
The value intended to be appended to the end of the array has to be included inside
the function's parenthesys.

var arr = [1,2,3];
arr.push(4);			Since i wanna add 4 to the end of the array, it 
// arr is now [1,2,3,4]    	needs to be added inside the function's parenthesys.

*/ 